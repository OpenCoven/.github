# Organization decisions

This directory contains decisions whose scope crosses repository ownership boundaries. Component-local implementation ADRs stay in the owning repository.

A decision record is `proposed` until merged through the protected review path. Acceptance coordinates organization behavior; it does not by itself authorize protected runtime changes, releases, publication, or GitHub administration.

Superseded decisions are retained with links to their successors. `decisions/index.json` is the machine-readable index.
